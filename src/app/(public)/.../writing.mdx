import ComponentContainer from "@/components/component-container";
import { Badge } from "@/components/dris/badge";
import { Button } from "@/components/dris/button";
import MdxLayout from "@/components/mdx-layout";
import { Snippet } from "@/components/snippet";
import { calculateAge, calculateDays, tenYears } from "@/lib/utils";

## ...

random thoughts, beliefs, and conclusions that could be wrong.

#### UPDATED 6/21/2026

- [impossible](/.../impossible) - what's impossible?
- [optimism](/.../optimism) - what can we control?

export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>;
}
