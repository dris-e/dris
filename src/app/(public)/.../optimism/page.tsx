import { Metadata } from "next";
import Optimism from "./optimism.mdx";

export const metadata: Metadata = {
  title: "OPTIMISM",
  description: "perception is the only thing we can control",
};

export default function OptimismPage() {
  return <Optimism />;
}
